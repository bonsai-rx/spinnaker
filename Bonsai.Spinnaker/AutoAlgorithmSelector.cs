using SpinnakerNET;

namespace Bonsai.Spinnaker
{
    // enum to select which auto algorithm is controlled by the
    // RoiEnable, OffsetX, OffsetY, Width, Height features
    public enum AutoAlgorithmSelector
    {
        AutoWhitebalance,
        AutoExposure
    }
}
